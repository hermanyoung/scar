# Ground truth for CWE-502 Deserialization corpus entry.
#
# Each finding declares what a correct security review should report.
# label: true_positive  — the model SHOULD flag this
# label: false_positive — the model should NOT flag this (hard negative)

cwe: "502"
language: csharp
description: "Unsafe deserialization via BinaryFormatter and Newtonsoft TypeNameHandling.All"

findings:
  - file: "DeserializationVuln.cs"
    line: 16
    cwe_id: "CWE-502"
    label: true_positive
    description: "BinaryFormatter.Deserialize called on untrusted Request.Body"
    sink: "BinaryFormatter.Deserialize"
    source: "Request.Body"

  - file: "DeserializationVuln.cs"
    line: 28
    cwe_id: "CWE-502"
    label: true_positive
    description: "JsonSerializerSettings with TypeNameHandling.All enables arbitrary type instantiation"
    sink: "JsonConvert.DeserializeObject"
    source: "TypeNameHandling.All setting"
