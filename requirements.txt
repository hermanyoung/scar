# Security Code Review Module — Python dependencies
# Install: pip install -r requirements.txt

# Core
pydantic>=2.7
pydantic-settings>=2.7
pyyaml>=6.0
click>=8.0
structlog>=24.0
rich>=13.0

# Code intelligence (C# AST parsing, call-graph-powered file selection)
tree-sitter>=0.23
tree-sitter-c-sharp>=0.23
rustworkx>=0.15
pyan3>=2.6              # Python call graph extraction (optional, degrades gracefully)

# LLM orchestration (PydanticAI + providers)
# Pin to the tested version (ADR-001 discipline: floating LLM-stack deps have
# bitten us before — A/B test before ANY upgrade).
pydantic-ai[openai,anthropic]==1.63.0

# Azure AI Foundry auth. The Foundry resource has disableLocalAuth=true, so
# there is no API key to resolve — inference authenticates with Entra ID
# bearer tokens sourced from DefaultAzureCredential.
azure-identity>=1.19

# Provider SDKs (pin to tested versions — breaking changes between minors)
# NOTE: 0.3.0 has a regression — CWE-312/522 return 0 findings (prompt truncation suspected)
github-copilot-sdk==0.2.2
claude-agent-sdk>=0.1.63
json-repair>=0.30

# SAST tools (Python security linter)
bandit[sarif]>=1.9.4

# Testing
pytest>=8.0
pytest-asyncio>=0.24
